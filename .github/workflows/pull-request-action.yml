name: Pull Request Action
run-name: Pull Request Action by ${{ github.actor }}

# This workflow is deprecated - see ci.yml for the unified CI pipeline
# Keeping this file for reference during transition

on:
  workflow_dispatch:  # Manual trigger only during transition

env:
  CI: true
  CONTAINER_TARGET: ci

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: echo "This workflow is deprecated. Please use ci.yml instead."
